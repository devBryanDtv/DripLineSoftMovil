<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (Modo Oscuro) -->
    <style name="Base.Theme.DripLineSoftApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Definir colorPrimary como cardview_dark_background -->
        <item name="colorPrimary">@color/cardview_dark_background</item>
        <item name="colorPrimaryVariant">@color/cardview_dark_background</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Personalización adicional -->
        <item name="android:statusBarColor">@color/cardview_dark_background</item>
        <item name="android:navigationBarColor">@color/cardview_dark_background</item>
    </style>
</resources>
